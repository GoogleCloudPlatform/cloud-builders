apiVersion: batch/v1
kind: Job
metadata:
  name: test-job
spec:
  template:
    metadata:
      labels:
        app: test-job
    spec:
      containers:
      - image: gcr.io/google-containers/busybox
        name: echo
        command: ["echo", "hi"]
      restartPolicy: Never
